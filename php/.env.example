# Database Configuration
# These values are automatically set in production via Docker build args
# For local development, copy to .env and update values

DB_HOST=localhost
DB_NAME=my_database
DB_USER=root
DB_PASSWORD=

# App URL (auto-configured in production)
APP_URL=http://localhost
