# HTML/CSS Project Template

A minimal template for pure HTML and CSS projects. No JavaScript, no PHP, no database - just static files served by nginx.

## How to Use
1. Edit index.html as your starting point
2. Add styles in styles.css or create additional CSS files
3. Add images and assets directly in the project root or subfolders
4. Push to trigger automatic build and deployment
